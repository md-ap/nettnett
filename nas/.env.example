# Secret compartido entre Vercel y el NAS para autenticar el webhook
WEBHOOK_SECRET=cambia-esto-por-un-secret-seguro

# Token del Cloudflare Named Tunnel (se obtiene al crear el tunnel en Zero Trust dashboard)
# Cloudflare Zero Trust -> Networks -> Tunnels -> tu tunnel -> Install connector -> copia el token
CLOUDFLARE_TUNNEL_TOKEN=tu-token-del-tunnel
